import { CardWithBridge } from "@/components/common/bridge-card";
import React from "react";

export default function Bridge() {
  return (
    <div className="flex w-full flex-col h-full items-center animate-in-reverse">
      <CardWithBridge />
    </div>
  );
}
